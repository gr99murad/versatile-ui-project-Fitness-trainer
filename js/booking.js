// function handleBooking() {
//     // Check if the user is logged in (using PHP session data)
//     const userLoggedIn = <?php echo isset($_SESSION['user_logged_in']) ? 'true' : 'false'; ?>;

//     if (userLoggedIn) {
//       // If logged in, proceed to booking page
//       window.location.href = './booking.php';  // Or any other booking URL
//     } else {
//       // If not logged in, redirect to login page
//       window.location.href = './login.php';  // Redirect to the login page
//     }
//   }